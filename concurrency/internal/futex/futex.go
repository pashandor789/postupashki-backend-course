// Package futex даёт две операции, на которых строятся блокировки:
// уснуть на адресе и разбудить спящих на этом адресе.
//
// Wait(addr, val) засыпает, если *addr всё ещё равен val. Проверка значения
// и засыпание происходят атомарно — иначе между ними успело бы вклиниться
// Wake, и поток уснул бы навсегда.
//
// Wake(addr) будит одного спящего, WakeAll — всех.
//
// На Linux это системный вызов futex(2). На остальных системах здесь
// эмуляция поверх стандартных средств Go: она нужна только чтобы задания
// собирались везде, и в саму домашку не входит.
package futex

var _ = Wait
